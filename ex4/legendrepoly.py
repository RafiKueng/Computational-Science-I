from derrint import fivepoint
import numpy as np


class LGP:
	def __init__(self, n, coef):
		self.n = n
		self.coef = coef
	
	def eval(x=0):
		n = len(self.coef) - 1
		sum = c[-1]
		for k in range(n, 0,-1) :
			sum = x * sum + c[k-1]
		return sum

	def prnt():
		print 'LGP'

def genLegendrePoly(n = 3):
	
	
	return False

 
if __name__ == '__main__':
	genLegendrePoly(5)